name: flint_build
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
